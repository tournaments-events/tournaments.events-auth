name: Deploy changes of the develop every night.

on:
  # Automatic nightly at 23:45 PM UTC
  schedule:
    - cron: '45 23 * * *'
  # Allow manually triggering nightly from the web interface
  workflow_dispatch:

jobs:
  publish_nightly:
    uses: sympauthy/sympauthy/.github/workflows/publish-nightly.yml@develop
