package tournament.events.auth.business.model.user

enum class UserInfoEnum {
    SUBJECT,

    NAME,
    GIVEN_NAME,
    FAMILY_NAME,
    MIDDLE_NAME,
    NICKNAME,

    PREFERRED_USERNAME,
    PROFILE,
    PICTURE,
    WEBSITE,

    EMAIL,
    EMAIL_VERIFIED,

    GENDER,
    BIRTH_DATE,

    ZONE_INFO,
    LOCALE,

    PHONE_NUMBER,
    PHONE_NUMBER_VERIFIED,

    UPDATED_AT
}
